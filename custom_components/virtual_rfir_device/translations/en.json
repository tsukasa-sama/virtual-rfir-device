{
  "config": {
    "step": {
      "user": {
        "title": "Add virtual RF/IR device",
        "description": "Create a virtual device that represents an appliance controlled through an existing IR/RF remote.",
        "data": {
          "name": "Name",
          "remote": "IR/RF remote"
        },
        "data_description": {
          "name": "Friendly name for the appliance (e.g. Family Room Fireplace).",
          "remote": "The remote entity (e.g. a Broadlink transmitter) used to send commands to this appliance."
        }
      }
    },
    "error": {
      "name_required": "A name is required."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Manage device",
        "description": "Add or remove the commands (buttons) and switches for this device.",
        "menu_options": {
          "add_command": "Add command",
          "remove_command": "Remove command",
          "add_switch": "Add switch",
          "remove_switch": "Remove switch",
          "finish": "Done"
        }
      },
      "add_command": {
        "title": "Add command",
        "description": "Each command becomes a button on this device.",
        "data": {
          "name": "Name",
          "icon": "Icon",
          "code": "IR/RF code"
        },
        "data_description": {
          "name": "Button name (e.g. Power, Brightness Up, Flicker).",
          "icon": "Optional icon for the button (e.g. mdi:fireplace).",
          "code": "The IR/RF code to transmit. Paste the raw Base64 code; a 'b64:' prefix is added automatically if missing."
        }
      },
      "remove_command": {
        "title": "Remove commands",
        "data": {
          "commands": "Commands to remove"
        }
      },
      "add_switch": {
        "title": "Add switch",
        "description": "Create an on/off switch from existing commands. For a toggle-only appliance, pick the same command for both on and off.",
        "data": {
          "name": "Name",
          "icon": "Icon",
          "on_command": "On command",
          "off_command": "Off command"
        },
        "data_description": {
          "name": "Switch name (e.g. Power).",
          "icon": "Optional icon for the switch (e.g. mdi:fireplace).",
          "on_command": "Command sent when the switch is turned on.",
          "off_command": "Command sent when the switch is turned off."
        }
      },
      "remove_switch": {
        "title": "Remove switches",
        "data": {
          "switches": "Switches to remove"
        }
      }
    },
    "error": {
      "name_required": "A name is required.",
      "code_required": "An IR/RF code is required."
    }
  }
}
